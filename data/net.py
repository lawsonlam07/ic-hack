from data.Coord import Coord


class Net:
    # This is a processed net location, with the x in our coordinate system
    def __init__(self, x: int):
        if len(corners) != 4:
            raise IndexError
        self.x = x